# Shared contracts

OpenAPI documents, JSON Schema, cross-platform fixtures, and `client-core/`
— the Kotlin game core compiled into both the Android and PC clients as a
shared source set (ADR-0020).

# Upload keystore (local only)

Generate `nullhorizon-upload.jks` here. The `*.jks` pattern in `.gitignore`
keeps key material out of git. Configure passwords in
`../keystore.properties` (also gitignored). See `docs/release/PLAY_PUBLISH.md`.
